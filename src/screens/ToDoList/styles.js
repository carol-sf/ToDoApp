import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    flex: 1,
    padding: 20
  },
  appTitle: {
    fontSize: 32,
    fontWeight: "bold"
  }
});

export default styles;