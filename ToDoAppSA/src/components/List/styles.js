import { StyleSheet } from "react-native"

const styles = StyleSheet.create({
    listTitle: {
      fontSize: 24,
      fontWeight: "bold",
      marginVertical: 20,
    }
});

export default styles;